# Example environment variables for this project
# Copy to .env, .env.local or .env.development and adjust values as needed.
# Vite exposes variables prefixed with VITE_ to the client code.

# Base URL for backend API used by src/services/movies.js
# Example: local development backend
VITE_API_BASE=http://localhost:4000

# Token storage keys used by client (localStorage)
# 默认客户端在 localStorage 中使用以下键名存储令牌：
# - 访问 token: jwt_token
# - 刷新 token: jwt_refresh
# 如果想使用不同的键名，请修改 src/services/auth.js 或在 README 中记录。

# 开发时可模拟用户角色（仅为示例）：
# 示例角色：admin, editor, guest
# 可在控制台手动设置 localStorage 用户信息进行调试：
# localStorage.setItem('user_info', JSON.stringify({ username: 'dev', role: 'admin', name: '开发者' }))
# 或者在调试脚本中导出示例角色变量：
# EXAMPLE_USER_ROLE=admin

# Example for production API
# VITE_API_BASE=https://api.example.com
